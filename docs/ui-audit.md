# Oigo native UI audit

BASE_SHA=f62686d6ccebd9806245483aa4904e95d116e8fd
TASK_01_SHA=e717d74a1d4bfbf16d5fb29496598191cd83cec2
AUDIT_SHA=a3c96c4f3d775ca9cd16ac55d5f68370fa24ab4d
CURRENT_HEAD_SHA=573a0eb6968df8f4f66f5729ac44a512fc35bce0
UNRELATED_WORKFLOW_RENAME=#140 ci: drop "master" from project workflow

## Reading this audit

The design artifacts named here describe approved intent and are not evidence that the native application implements or renders that intent.
The source owner is the current authority at `CURRENT_HEAD_SHA`; a mismatch states the work still owned by the listed child issue.
Data disposition excludes transcript bodies, audio, clipboard values, dictionary terms, focused-element text, and target coordinates from the future presentation projection unless a row explicitly names an existing durable owner.
`DETERMINISTIC PASS` identifies an executable or source-contract seam only.
`NATIVE INCONCLUSIVE` means an app-bundle and the corresponding observed AppKit behavior are still required.
No row below makes a native claim from HTML, grep, or SwiftPM output.

| row | design state or action | authoritative current source owner | command or action owner | data sensitivity and disposition | mismatch at current head | child issue | deterministic or native evidence seam |
| --- | --- | --- | --- | --- | --- | --- | --- |
| status-idle | `Oigo Icon.dc.html` idle template mark | `capturePresentationInputs()` -> `OigoPresentationState.project()` -> `OigoStatusIdentityArtwork` | `OigoAppDelegate.configureStatusItem()` | Operational state only; no session body | The typed mark exists, but the production status item still uses the literal title until #133 wires the renderer | #133 | DETERMINISTIC PASS: `Tests/OigoNativeUIContractTests/Scenarios/task-09/IdentityScenario.swift`; NATIVE INCONCLUSIVE: app-bundle status-item observation |
| status-activity | `State Boards.dc.html` processing activity | `OigoPresentationState.menuMark` and `OigoStatusIdentityArtwork` | `startDictation` and coordinator lifecycle | Phase enum only; no preview text | The typed activity mark and visible-only animation exist, but production renderer wiring remains in #133 | #133 | DETERMINISTIC PASS: presentation matrix and identity contracts; NATIVE INCONCLUSIVE: visible activity bounded to work |
| status-recording | `Oigo Icon.dc.html` recording badge | `OigoPresentationState.menuMark` and `OigoStatusIdentityArtwork` | `handleMouseToggle` and `requestKeyboardStop` | Recording phase and duration only | The typed recording mark exists, but production status-item rendering remains in #133 | #133 | DETERMINISTIC PASS: coordinator and identity contracts; NATIVE INCONCLUSIVE: light/dark menu-bar rendering |
| status-attention | `State Boards.dc.html` attention mark | `OigoPresentationState.menuMark` and `OigoPresentationState.prioritizedNotice` | `retryStorage`, Settings routes | Health category only; no error payload | The single adapter now owns the attention category; native differentiation and renderer wiring remain in #133 | #131, #133 | DETERMINISTIC PASS: presentation matrix and publication contracts; NATIVE INCONCLUSIVE: non-color native differentiation |
| menu-start-stop | `screen-map.md` primary dictation action | `OigoPresentationState.primaryAction` published by `OigoPresentationPublication` | `handleMouseToggle` | Command eligibility and typed reason only | The typed primary action is published to the status surface; native primary-click proof remains in #134 | #134 | DETERMINISTIC PASS: `task-03`, `task-04`, and `task-05` contracts; NATIVE INCONCLUSIVE: primary click behavior |
| menu-settings-history-quit | `screen-map.md` secondary utility menu | `StatusSurfaceController` and typed `OigoStatusSurfaceCommand` | `openSettings`, `openHistory`, `quit` | No user content | The utility menu is now owned by the status surface controller; native secondary-click proof remains in #134 | #134 | DETERMINISTIC PASS: `Tests/OigoNativeUIContractTests/Scenarios/task-11/PopoverRoutingScenario.swift`; NATIVE INCONCLUSIVE: secondary-click menu |
| popover-ready | `Popover + HUD Prototype.dc.html` Ready | `OigoPresentationState` -> `OigoPopoverPresentation.compose()` | `handleMouseToggle` | Bounded operational state and shortcut label | The transient popover and state composition now exist; native rendering proof remains in #134 | #134 | DETERMINISTIC PASS: `task-12/PopoverStateMatrixScenario.swift`; NATIVE INCONCLUSIVE: transient popover |
| popover-recording | `Popover + HUD Prototype.dc.html` Recording | `OigoPresentationState` -> `OigoPopoverPresentation.compose()` | `requestKeyboardStop` | Phase and shortcut hint only | The recording variant is typed and composed; native disabled/enabled control proof remains in #134 | #134 | DETERMINISTIC PASS: presentation matrix contract; NATIVE INCONCLUSIVE: disabled/enabled native controls |
| popover-processing | `Popover + HUD Prototype.dc.html` phase labels | `OigoPresentationState` -> `OigoPopoverPresentation.compose()` | Coordinator terminalization | Phase enum only | Processing labels are composed from the adapter; native phase rendering proof remains in #134 | #134 | DETERMINISTIC PASS: presentation matrix contract; NATIVE INCONCLUSIVE: visible phase rendering |
| popover-storage-unavailable | `ui-state-matrix.md` Storage unavailable | `OigoPresentationState.prioritizedNotice` from `DurableSessionCapability.health` | Storage retry callback in `OigoAppDelegate` | Health category only; no filesystem path | Notice priority and typed recovery action are now adapter-owned; native notice proof remains in #134 | #134 | DETERMINISTIC PASS: presentation matrix and popover contracts; NATIVE INCONCLUSIVE: Retry/Open Data Location notice |
| popover-shortcut-inactive | `ui-state-matrix.md` shortcut notice | `OigoPresentationState.prioritizedNotice` from shortcut transaction status | `openSettings` | Registration category and safe reason only | Notice priority and mouse-start availability are now adapter-owned; native notice proof remains in #134 | #134 | DETERMINISTIC PASS: presentation matrix and popover contracts; NATIVE INCONCLUSIVE: notice/action exposure |
| popover-microphone-unavailable | `ui-state-matrix.md` microphone notice | `OigoPresentationState.prioritizedNotice` from permission input | Settings microphone route | Permission state only | The typed notice and disabled action are adapter-owned; native system-settings proof remains in #134 | #134 | DETERMINISTIC PASS: presentation matrix and popover contracts; NATIVE INCONCLUSIVE: system-settings route |
| popover-input-unavailable | `ui-state-matrix.md` pinned input unavailable | `OigoPresentationState.prioritizedNotice` from input selection | Settings input selection | Device identifier/status only; never substitute | The typed unavailable-input notice is adapter-owned; native choice proof remains in #134 | #134 | DETERMINISTIC PASS: presentation matrix and popover contracts; NATIVE INCONCLUSIVE: disabled action and choice |
| popover-assets-unavailable | `ui-state-matrix.md` assets unavailable | `OigoPresentationState.prioritizedNotice` from locale asset status | `inspectSpeechAssets` through Settings | Locale identifier and asset state only | The typed assets notice is adapter-owned; native install/settings proof remains in #134 | #134 | DETERMINISTIC PASS: presentation matrix and popover contracts; NATIVE INCONCLUSIVE: install/open Settings behavior |
| popover-accessibility-copy-only | `ui-state-matrix.md` Copy-only | `OigoPresentationState.prioritizedNotice` and `copyOnlyPosture` | `startDictation` and paste fallback | Copy-only category; no focused field or clipboard text | The calm copy-only state is adapter-owned; native notice proof remains in #134 | #134 | DETERMINISTIC PASS: presentation matrix and popover contracts; NATIVE INCONCLUSIVE: copy-only notice |
| popover-retry-required | `ui-state-matrix.md` retry required | `OigoPresentationState` terminal and latest-session projections | `HistoryWindowController.retryTranscriptionAction` | Bounded session metadata and retry category | The retry state and action are adapter-owned; native retry proof remains in #134 | #134 | DETERMINISTIC PASS: presentation matrix and popover contracts; NATIVE INCONCLUSIVE: retry action |
| popover-last-session-actions | `screen-map.md` Last dictation Copy/Paste Again | `OigoPresentationState.latestActions` and bounded latest-session input | Copy and insertion handoff callbacks | Metadata only; never transcript body in popover | The bounded latest-session projection is now published; native metadata-only proof remains in #134 | #134 | DETERMINISTIC PASS: presentation matrix and popover contracts; NATIVE INCONCLUSIVE: metadata-only popover |
| popover-next-configuration | `screen-map.md` Applies to next dictation | `OigoPresentationState.nextDictationNotice` and publication adapters | `applySettings` | Settings values only; no transcript | The active-versus-next distinction is now adapter-owned; native caption proof remains in #134 | #134 | DETERMINISTIC PASS: presentation matrix and publication contracts; NATIVE INCONCLUSIVE: caption visibility |
| hud-preparing | `State Boards.dc.html` Preparing | `OigoPresentationState.hud` -> `OigoHUDController` | `capturePresentationInputs()` and coordinator lifecycle | Phase only | HUD policy and controller now cover the state; native field-relative proof remains in #135 | #135 | DETERMINISTIC PASS: `Tests/OigoNativeUIContractTests/Scenarios/task-14/HUDPlacementScenario.swift`; NATIVE INCONCLUSIVE: field-relative panel |
| hud-recording | `screen-map.md` Recording timer and preview | `OigoHUDController` and `OigoHUDShellPolicy` | Coordinator recording lifecycle | Timer and bounded volatile preview; never durable transcript | The controller now uses a 1 Hz timer and bounded preview; native placement proof remains in #135 | #135 | DETERMINISTIC PASS: HUD placement/state contracts; NATIVE INCONCLUSIVE: 1 Hz field-relative HUD |
| hud-processing | `screen-map.md` Finalizing/Cleaning/Pasting | `OigoPresentationState.hud` and `OigoHUDShellPolicy` | `publishPresentation()` | Phase only | The complete policy vocabulary exists; native phase presentation proof remains in #135 | #135 | DETERMINISTIC PASS: HUD state contract; NATIVE INCONCLUSIVE: complete state presentation |
| hud-paste-attempted | `ui-state-matrix.md` Paste attempted | `OigoPresentationState.terminal` and `OigoHUDShellPolicy` | insertion completion in `OigoAppDelegate` | Outcome category only; never infer verified paste | The distinct typed outcome is published to the HUD; native terminal proof remains in #135 | #135 | DETERMINISTIC PASS: presentation and HUD contracts; NATIVE INCONCLUSIVE: terminal display |
| hud-copy-only | `ui-state-matrix.md` Copied to Clipboard | `OigoPresentationState.copyOnlyPosture` and `OigoHUDShellPolicy` | insertion fallback | Copy-only category; no clipboard value | The calm copy-only policy exists; native terminal proof remains in #135 | #135 | DETERMINISTIC PASS: presentation and HUD contracts; NATIVE INCONCLUSIVE: visual terminal treatment |
| hud-retry-required | `ui-state-matrix.md` Recording Saved - Retry Needed | `OigoPresentationState.terminal` and `OigoHUDShellPolicy` | retry command owner | Retry category and durable-session presence only | The retry-required policy exists; native actionable-terminal proof remains in #135 | #135 | DETERMINISTIC PASS: presentation and HUD contracts; NATIVE INCONCLUSIVE: timed actionable terminal |
| hud-preserved-failure | `screen-map.md` Failed - Recording Preserved | `OigoPresentationState.terminal` and `OigoHUDShellPolicy` | recovery command owner | Outcome category and bounded metadata | The preserved-failure policy exists; native recovery-terminal proof remains in #135 | #135 | DETERMINISTIC PASS: presentation and HUD contracts; NATIVE INCONCLUSIVE: recovery terminal |
| hud-cancelled | `ui-state-matrix.md` Cancelled before/after durable raw text | `OigoPresentationState.terminal` and `OigoHUDShellPolicy` | cancellation path | Cancellation category; durable presence only | Both cancellation classes are typed and published; native cancellation proof remains in #135 | #135 | DETERMINISTIC PASS: presentation and HUD contracts; NATIVE INCONCLUSIVE: cancellation display |
| hud-interrupted | `ui-state-matrix.md` Interrupted | `OigoPresentationState.terminal` and `OigoHUDShellPolicy` | workspace interruption observer | Interruption category and audio-preserved flag only | The interrupted policy exists; native recovery-terminal proof remains in #135 | #135 | DETERMINISTIC PASS: presentation and HUD contracts; NATIVE INCONCLUSIVE: recovery terminal |
| onboarding-storage | `Onboarding.dc.html` Mac & Storage | `OigoOnboardingEvidenceMachine`, `OigoOnboardingStage`, and storage capability | `showOnboarding` and retry route | Support/storage status only; no data path | Four-stage presentation owns the first stage while storage truth remains in the existing capability | #136 | DETERMINISTIC PASS: `onboarding-flow` migration/stage contract; NATIVE INCONCLUSIVE: native stage layout |
| onboarding-microphone-language | `Onboarding.dc.html` Microphone & Language | onboarding evidence, input catalog, asset result | source probe and locale check callbacks | Permission/device/channel/locale status only | Microphone, channel, meter, language, and asset readiness now share one stage | #136 | DETERMINISTIC PASS: `onboarding-flow` plus issue-90 and asset contracts; NATIVE INCONCLUSIVE: stage gating |
| onboarding-shortcut-insertion | `Onboarding.dc.html` Shortcut & Insertion | shortcut transaction, Accessibility state, and persisted copy-only evidence | registration callbacks | Shortcut candidate/status and copy-only category only | Shortcut recording and insertion posture now share one stage with explicit copy-only acceptance | #136 | DETERMINISTIC PASS: `onboarding-flow` plus issue-82 and insertion contracts; NATIVE INCONCLUSIVE: transactional recorder |
| onboarding-try-it | `Onboarding.dc.html` Try It | `OigoOnboardingEvidenceMachine` | `OnboardingWindowController.startTest` closure (lines 719-742) -> `OigoAppDelegate` `startTest` closure (lines 484-486) -> `beginOnboardingProductionTest(generation:)` (line 2488); `OnboardingWindowController.applyTestCompletion(generation:sessionID:report:)` (line 239) | Oigo-owned synthetic field and evidence outcome; no third-party field content | Current test workflow is implementation-oriented | #136 | DETERMINISTIC PASS: onboarding evidence contract; NATIVE INCONCLUSIVE: owned-field interaction |
| onboarding-done | `Onboarding.dc.html` Done | `OigoOnboardingStore` completion state and committed settings | `markCompleted` | Completion metadata only | Done summarizes committed shortcut, mode, input/channel, locale, insertion posture, test outcome, and local storage | #136 | DETERMINISTIC PASS: `onboarding-flow` persistence contract; NATIVE INCONCLUSIVE: summary window |
| settings-general | `Settings.dc.html` General pane | `OigoSettings` and launch-at-login status | `saveSettings`, `rerunOnboardingAction` | Settings and launch state only | Current settings has one stacked global Save form | #137 | DETERMINISTIC PASS: settings transaction contract; NATIVE INCONCLUSIVE: toolbar pane |
| settings-dictation | `Settings.dc.html` Dictation pane | `OigoSettings`, input catalog, asset state | input/channel/locale actions | Device and locale identifiers only; pinned input preserved | Current controls are monolithic | #137 | DETERMINISTIC PASS: issue-90 and asset contracts; NATIVE INCONCLUSIVE: pane behavior |
| settings-dictionary | `Settings.dc.html` Dictionary pane | `DictionaryDocument` and `DictionaryStore` | `persistDictionaryEntries` | Dictionary entries are sensitive local content; future projection has counts/status only | Existing editor is embedded in one form | #137 | DETERMINISTIC PASS: issue-13 dictionary contract; NATIVE INCONCLUSIVE: split editor |
| settings-data-privacy | `Settings.dc.html` Data & Privacy pane | permissions, storage, retention, diagnostics | history/data-folder/retry/export/delete actions | Health and retention summary only; diagnostics must stay sanitized | Current actions are in one form | #137 | DETERMINISTIC PASS: storage/privacy contracts; NATIVE INCONCLUSIVE: pane and confirmation sheet |
| dictionary-actions | `Settings.dc.html` add/edit/toggle/delete/test normalization | `SettingsWindowController` and `DictionaryStore` | dictionary action methods and normalization | Dictionary text remains local and excluded from UI logs/evidence | Designed split editor is not implemented | #137 | DETERMINISTIC PASS: issue-13 contract; NATIVE INCONCLUSIVE: editor accessibility |
| history-list-detail | `History.dc.html` bounded list and detail | `HistoryWindowController` and `SessionStore` | `reload`, selection, transcript load | Bounded metadata list; selected transcript only; no bodies for unselected rows | Current table/detail layout differs from split workspace | #138 | DETERMINISTIC PASS: issue-7 contract; NATIVE INCONCLUSIVE: split view |
| history-toolbar-actions | `History.dc.html` Copy/Play/More | `HistoryWindowController` command callbacks | copy, playback, clean, dictionary, retry, reveal, delete actions | Selected-session capability only; transcript values stay local | Current button-heavy layout lacks designed toolbar | #138 | DETERMINISTIC PASS: issue-92 and command contracts; NATIVE INCONCLUSIVE: toolbar routing |
| history-paste-again | `History.dc.html` safe destination handoff | `InsertionTargetHandoff` and app insertion flow | `pasteAgainAction` | No target coordinates or focused text retained | Existing action lacks designed HUD handoff presentation | #135, #138 | DETERMINISTIC PASS: issue-9/10 contracts; NATIVE INCONCLUSIVE: destination handoff |
| history-stale-load | `History.dc.html` stale-generation fencing | `HistoryLoadGeneration`, controller generation counters, and the published presentation generation | `refreshHistory` and selection loading | Generation number only; no stale session content | Existing History fencing remains controller-owned pending #138; #131 supplies the shared publication generation but does not rewrite History | #131, #138 | DETERMINISTIC PASS: issue-7 and publication contracts; NATIVE INCONCLUSIVE: no stale native repaint |
| shutdown | `ui-state-matrix.md` Quitting | `OigoAppDelegate.applicationShouldTerminate`, `AppOperationGate`, and `OigoPresentationGenerationFence` | `quit`, `finishApplicationTermination` | Lifecycle state only; all panels/tasks released | The publication fence now rejects state after shutdown; app-bundle lifecycle proof remains in #139 | #139 | DETERMINISTIC PASS: publication and coordinator shutdown contracts; NATIVE INCONCLUSIVE: app-bundle quit cleanup |
| stale-generation | `ui-state-matrix.md` newer state wins | `OigoPresentationGenerationFence` plus StatusSurfaceController, History, and onboarding generation guards | async completion guards | Generation tokens only; never old result content | The shared publication boundary prevents stale status/popover publication; later surfaces retain their own guards until their child issues | #131, #135, #136, #138, #139 | DETERMINISTIC PASS: `task-05` and related generation contracts; NATIVE INCONCLUSIVE: cross-surface replacement |

## Current implementation boundary

The current head contains the immutable `OigoPresentationState` adapter, typed publication boundary, generic `MacUtilityUI` layer, identity source and renderer, transient popover, HUD placement and state policy, Paste Again handoff contracts, and the four-stage onboarding flow.
`OigoAppDelegate.capturePresentationInputs()` constructs one presentation state, and `publishPresentation()` sends that immutable publication to the instantiated surfaces behind `OigoPresentationGenerationFence`.
The native visual proof, four-pane Settings, and split History workspace remain owned by the later child issues listed in this audit.
Issue #140 is unrelated workflow terminology cleanup at the recorded base and does not alter this audit's product behavior boundary.

## #136 implementation evidence

- `Sources/OigoCore/ProductContracts.swift` defines the four user-facing stages plus Done and maps every legacy persisted step to its canonical entry step.
- `Sources/OigoCore/ProductContracts.swift` preserves old onboarding records, decodes the new copy-only evidence field when present, and defaults older records safely.
- `Sources/Oigo/OnboardingWindowController.swift` renders the combined Microphone & Language and Shortcut & Insertion stages, starts source probing only while the combined stage is visible, persists explicit copy-only acceptance, renders the evidence-backed nine-row Try It checklist, and shows the committed Done summary.
- `Tests/OigoNativeUIContractTests/Scenarios/task-17/OnboardingFlowScenario.swift` exercises all eight legacy steps, backward decoding, copy-only persistence, stage titles, and stage transitions.
- The deterministic contract and `swift build --product Oigo` pass; native permission, source, owned-field, keyboard, VoiceOver, and appearance observations remain `NATIVE INCONCLUSIVE` until the real bundle matrix is run.

## #131 implementation evidence at `CURRENT_HEAD_SHA`

- `Sources/Oigo/UI/Presentation/OigoPresentationInputs.swift` contains value-only snapshots of operation, storage, permissions, shortcut, input, locale, configuration, terminal, latest-session, playback, onboarding, and shutdown inputs.
- `Sources/Oigo/UI/Presentation/OigoPresentationState.swift` contains the typed surface rows, actions, notices, terminal classes, HUD policies, availability, and copy-only posture.
- `Sources/Oigo/UI/Presentation/OigoPresentationProjection.swift:2` is the single product projection entry point.
- `Sources/Oigo/UI/Presentation/OigoPresentationAttributes.swift` owns deterministic mark, status, action, notice-priority, HUD, availability, and terminal mappings.
- `Sources/Oigo/UI/Presentation/OigoPresentationPublication.swift` owns immutable publication adapters and the monotonic generation fence without timers, observers, tasks, filesystem access, Accessibility objects, or AppKit views.
- `Sources/Oigo/OigoAppDelegate.swift:2020-2129` captures authoritative snapshots, projects one state, and publishes it to the instantiated surfaces.
- `Tests/OigoNativeUIContractTests/Scenarios/task-02/AuditCompletenessScenario.swift`, `task-03/PresentationInputsScenario.swift`, `task-04/PresentationMatrixScenario.swift`, and `task-05/AppDelegatePublicationScenario.swift` are the focused deterministic verification seams.
- The audit intentionally records `NATIVE INCONCLUSIVE` where later child work still requires app-bundle observation, and it does not claim native success from HTML, grep, or SwiftPM output.
